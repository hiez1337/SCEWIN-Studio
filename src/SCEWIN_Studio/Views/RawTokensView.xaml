<UserControl x:Class="SCEWIN_Studio.Views.RawTokensView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:controls="clr-namespace:SCEWIN_Studio.Views.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{Binding L10n[Nav_RawTokens]}"
                       FontWeight="Bold"
                       FontSize="24"
                       Foreground="#FFFFFF" />
            <TextBlock Text="Полный список всех обнаруженных переменных Setup Question в дампе NVRAM с возможностью мгновенного поиска и фильтрации."
                       FontSize="13"
                       Margin="0,4,0,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Filter Bar -->
        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Search box -->
                <TextBox Grid.Column="0"
                         Text="{Binding RawSearchQuery, UpdateSourceTrigger=PropertyChanged}"
                         ui:ControlHelper.PlaceholderText="{Binding L10n[Raw_SearchPlaceholder]}"
                         ui:ControlHelper.CornerRadius="6"
                         Height="34"
                         Margin="0,0,16,0" />

                <!-- Category filter -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="{Binding L10n[Raw_CategoryFilter]}" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ComboBox SelectedValue="{Binding RawCategoryFilter, Mode=TwoWay}"
                              SelectedValuePath="Tag"
                              MinWidth="200"
                              Height="34">
                        <ComboBoxItem Content="Все категории" Tag="All" IsSelected="True" />
                        <Separator />
                        <ComboBoxItem Content="PCIe &amp; ASPM" Tag="ASPM" />
                        <ComboBoxItem Content="AMD Разгон / PBO" Tag="Overclocking" />
                        <ComboBoxItem Content="Память и Тайминги" Tag="Memory" />
                        <ComboBoxItem Content="Процессор и C-States" Tag="CpuPower" />
                        <ComboBoxItem Content="Охлаждение и VRM" Tag="CoolingPower" />
                        <ComboBoxItem Content="Встроенная периферия" Tag="Peripherals" />
                        <ComboBoxItem Content="Загрузка и безопасность" Tag="BootSecurity" />
                        <ComboBoxItem Content="Прочие токены" Tag="Other" />
                        <Separator />
                        <ComboBoxItem Content="• PBO &amp; Curve Optimizer" Tag="PboCurve" />
                        <ComboBoxItem Content="• Лимиты мощности (PPT/TDC)" Tag="PboLimits" />
                        <ComboBoxItem Content="• C-States и энергосбережение" Tag="CpuPowerStates" />
                        <ComboBoxItem Content="• Топология ядер и SMT" Tag="CpuTopology" />
                        <Separator />
                        <ComboBoxItem Content="• Первичные тайминги" Tag="PrimaryTimings" />
                        <ComboBoxItem Content="• Вторичные тайминги" Tag="SecondaryTimings" />
                        <ComboBoxItem Content="• Третичные тайминги" Tag="TertiaryTimings" />
                        <ComboBoxItem Content="• Сопротивления &amp; GDM" Tag="TerminationsGdm" />
                        <ComboBoxItem Content="• Напряжения памяти" Tag="Voltages" />
                        <ComboBoxItem Content="• Частоты &amp; Шина" Tag="Frequency" />
                        <Separator />
                        <ComboBoxItem Content="• ASPM &amp; L1 Substates" Tag="AspmL1" />
                        <ComboBoxItem Content="• Bifurcation &amp; Скорость" Tag="BifurcationSpeed" />
                        <ComboBoxItem Content="• Re-Size BAR &amp; Above 4G" Tag="ReBar" />
                        <Separator />
                        <ComboBoxItem Content="• Профили вентиляторов" Tag="FanProfiles" />
                        <ComboBoxItem Content="• Температурные лимиты" Tag="ThermalLimits" />
                        <ComboBoxItem Content="• Питание VRM" Tag="VrmPower" />
                        <Separator />
                        <ComboBoxItem Content="• Сетевые адаптеры" Tag="Network" />
                        <ComboBoxItem Content="• USB &amp; Thunderbolt" Tag="UsbThunderbolt" />
                        <ComboBoxItem Content="• Звук &amp; Подсветка" Tag="AudioRgb" />
                        <Separator />
                        <ComboBoxItem Content="• Параметры загрузки" Tag="BootParams" />
                        <ComboBoxItem Content="• Безопасность &amp; TPM" Tag="SecurityTpm" />
                        <ComboBoxItem Content="• Накопители &amp; RAID" Tag="StorageRaid" />
                    </ComboBox>
                </StackPanel>

                <!-- Only modified checkbox -->
                <CheckBox Grid.Column="2"
                          Content="{Binding L10n[Raw_OnlyModified]}"
                          IsChecked="{Binding RawOnlyModified, Mode=TwoWay}"
                          VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Fully Virtualized ListView of TokenCards -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding FilteredRawTokens}"
                  Background="Transparent"
                  BorderThickness="0"
                  Padding="0"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  VirtualizingPanel.CacheLength="2">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,10" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:TokenCard />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
