<UserControl x:Class="SCEWIN_Studio.Views.PciePowerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:controls="clr-namespace:SCEWIN_Studio.Views.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="1000">
            <!-- Header -->
            <TextBlock Text="{Binding L10n[Pcie_PageTitle], FallbackValue='PCIe &amp; Latency Optimization'}"
                       FontWeight="Bold"
                       FontSize="24"
                       Foreground="#FFFFFF" />
            <TextBlock Text="{Binding L10n[Pcie_AspmDesc]}"
                       FontSize="13"
                       Margin="0,6,0,16"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <!-- SubCategory Filter Pills -->
            <WrapPanel Margin="0,0,0,16">
                <Button Content="{Binding L10n[Filter_All]}"
                        Command="{Binding SetPcieSubCategoryCommand}"
                        CommandParameter="All">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FluentPillFilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PcieSubCategory}" Value="All">
                                    <Setter Property="Background" Value="#1C384F" />
                                    <Setter Property="BorderBrush" Value="#60CDFF" />
                                    <Setter Property="Foreground" Value="#60CDFF" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="{Binding L10n[Filter_AspmL1]}"
                        Command="{Binding SetPcieSubCategoryCommand}"
                        CommandParameter="AspmL1">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FluentPillFilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PcieSubCategory}" Value="AspmL1">
                                    <Setter Property="Background" Value="#1C384F" />
                                    <Setter Property="BorderBrush" Value="#60CDFF" />
                                    <Setter Property="Foreground" Value="#60CDFF" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="{Binding L10n[Filter_BifurcationSpeed]}"
                        Command="{Binding SetPcieSubCategoryCommand}"
                        CommandParameter="BifurcationSpeed">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FluentPillFilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PcieSubCategory}" Value="BifurcationSpeed">
                                    <Setter Property="Background" Value="#1C384F" />
                                    <Setter Property="BorderBrush" Value="#60CDFF" />
                                    <Setter Property="Foreground" Value="#60CDFF" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="{Binding L10n[Filter_ReBar]}"
                        Command="{Binding SetPcieSubCategoryCommand}"
                        CommandParameter="ReBar">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource FluentPillFilterButtonStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding PcieSubCategory}" Value="ReBar">
                                    <Setter Property="Background" Value="#1C384F" />
                                    <Setter Property="BorderBrush" Value="#60CDFF" />
                                    <Setter Property="Foreground" Value="#60CDFF" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </WrapPanel>

            <!-- Tokens list -->
            <ItemsControl ItemsSource="{Binding FilteredAspmTokens}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:TokenCard />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
